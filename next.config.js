/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // <-- Add this exact line
  // ... leave your other existing rules (like images, components, etc.) exactly as they are
};

module.exports = nextConfig;

